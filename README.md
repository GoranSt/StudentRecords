# StudentRecords - Single Page Application


ASP.NET WEB API, EF(code first), jQuery and Bootstrap


Before debug, run <code>update-database</code> in console to call seed method(for adding examples in db)
